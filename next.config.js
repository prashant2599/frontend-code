/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['dev.medflick.com',"medflick-frontend.s3.ap-south-1.amazonaws.com"],
    },

  };
  
  module.exports = nextConfig;
  